<%= form_for(@favorite_class) do |f| %>
  <% if @favorite_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favorite_class.errors.count, "error") %> prohibited this favorite_class from being saved:</h2>

      <ul>
      <% @favorite_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="jumbotron">
  <p>
      <%= f.hidden_field :user_id, value: session[:user_id] %>

      <div class="field form-group">
        <%= f.label :dance_class_id, class: 'control-label' %><br>
        <div class="col-lg-4">
          <%= f.select(:dance_class_id, options_from_collection_for_select(DanceClass.all, :id, :name), {}, {:class =>"form-control"}) %>
        </div>
      </div>

      <div class="form-actions">
        <div class="col-lg-offset-5 col-lg-7">
          <%= f.submit :class => 'btn btn-success btn-large'%>
        </div>
      </div>
    <% end %>
    </div>
  </p>
</div>

