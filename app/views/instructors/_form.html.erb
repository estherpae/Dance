<%= form_for(@instructor) do |f| %>
  <% if @instructor.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <button type="button" class="close" data-dismiss="alert">&times;</button>
      <h2><%= pluralize(@instructor.errors.count, "error") %> prohibited this instructor from being saved:</h2>

      <ul>
      <% @instructor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="jumbotron">
  <p>
  <div class="form-horizontal">
    <div class="field form-group">
      <%= f.label :name, class: 'control-label' %><br>
      <div class="col-lg-4">
        <%= f.text_field :name, class: "form-control" %>
      </div>
    </div>
    <div class="field form-group">
      <%= f.label :style, class: 'control-label' %><br>
      <div class="col-lg-4">
        <%= f.text_field :style, class: "form-control" %>
      </div>
    </div>
    <div class="field form-group">
      <%= f.label :biography, class: 'control-label' %><br>
      <div class="col-lg-4">
        <%= f.text_area :biography, class: "form-control" %>
      </div>
    </div>

    <div class="form-actions">
      <div class="col-lg-offset-5 col-lg-7">
        <%= f.submit :class => 'btn btn-success btn-large'%>
      </div>
    </div>
  <% end %>
  </div>
  </p>
</div>


